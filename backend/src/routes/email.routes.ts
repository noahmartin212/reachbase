import { Router } from 'express';
// import { authenticate } from '../middleware/auth';
// import { emailLimiter } from '../middleware/rateLimiter';
// import * as emailController from '../controllers/email.controller';

const router = Router();

// Routes to be implemented:
// router.get('/', authenticate, emailController.getEmails);
// router.get('/:id', authenticate, emailController.getEmailById);
// router.post('/send', authenticate, emailLimiter, emailController.sendEmail);
// router.get('/:id/events', authenticate, emailController.getEmailEvents);

export default router;
