import { Router } from 'express';
// import { authenticate } from '../middleware/auth';
// import * as analyticsController from '../controllers/analytics.controller';

const router = Router();

// Routes to be implemented:
// router.get('/dashboard', authenticate, analyticsController.getDashboard);
// router.get('/sequences', authenticate, analyticsController.getSequenceAnalytics);
// router.get('/emails', authenticate, analyticsController.getEmailAnalytics);
// router.get('/team', authenticate, analyticsController.getTeamAnalytics);

export default router;
