import { Router } from 'express';
// import { authenticate } from '../middleware/auth';
// import * as sequenceController from '../controllers/sequence.controller';

const router = Router();

// Routes to be implemented:
// router.get('/', authenticate, sequenceController.getSequences);
// router.post('/', authenticate, sequenceController.createSequence);
// router.get('/:id', authenticate, sequenceController.getSequenceById);
// router.put('/:id', authenticate, sequenceController.updateSequence);
// router.delete('/:id', authenticate, sequenceController.deleteSequence);
// router.post('/:id/enroll', authenticate, sequenceController.enrollContacts);
// router.post('/:id/pause', authenticate, sequenceController.pauseSequence);
// router.post('/:id/resume', authenticate, sequenceController.resumeSequence);

export default router;
